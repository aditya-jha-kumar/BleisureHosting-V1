import React from "react";
import {
  FaMapMarkerAlt,
  FaCalendarAlt,
  FaUser,
  FaArrowRight,
} from "react-icons/fa";

interface HeroProps {
  smallText: string;
  largeText: string;
  backgroundImage: string;
  className?: string; // Adding className prop
}

const Hero = ({
  smallText,
  largeText,
  backgroundImage,
  className,
}: HeroProps) => {
  return (
    <div>

    </div>
    
  );
};

export default Hero;
